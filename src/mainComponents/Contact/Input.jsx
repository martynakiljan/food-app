/** @format */

const Input = ({ labelName, label, register, required }) => (
  <>
    <label className="form__label">
      <input className="form__input" />
    </label>
  </>
);

export default Input;
